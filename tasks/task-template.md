# Task / Requirements
<What should be built/changed — functional requirements, without anticipating the technical implementation.>

# Context
- Affected modules: <e.g., 01-simple-filter to 04-advanced-ai-filter>
- Purpose: <e.g., "Demo for a banking audience at a JUG talk" — influences code style decisions>
- Relevant existing parts: <Classes/files that serve as a model or are affected>

# Approach
1. First, create a plan (Plan Mode) and present it to me. Review the requirements for alternatives or any inconsistencies with the existing code. Wait for my approval.
2. Implement the plan. Work in logical increments, committing after each verified step (Conventional Commits, do not push).
3. Verify your work independently according to the Definition of Done in `CLAUDE.md`. Iterate on errors autonomously — do not present error messages to me for analysis if you can reproduce and fix them yourself.
4. Update the `README.md` and `CLAUDE.md` files to reflect the current state of the project. Delete outdated information.

# Definition of Done (in addition to CLAUDE.md)
- <Task-specific acceptance criteria, e.g., "Filter query X returns result Y">
- <UI criteria, e.g., "Traffic light component aligned to the right, screenshot provided as proof">
- <Technical criteria, e.g., "all tests pass, no warnings, no code redundancy">

# Final Report
At the end, provide a summary of: what was changed (per commit), which tests are passing, and any open points/decisions that I need to make.